apiVersion: scaffolder.backstage.io/v1beta3
kind: Template

metadata:
  name: doc-basis-template
  title: A More Human-Readable Title
  description: This template does nothing and has no usage but being example in the documentation.
  labels:
    example.com/custom: custom_label_value
  annotations:
    example.com/service-discovery: artistweb
    circleci.com/project-slug: github/example-org/artist-website
  tags:
    - example
    - gold
    - edu
  links:
    - url: https://admin.example-org.com
      title: Admin Dashboard
      icon: dashboard
      type: admin-dashboard

spec:
  type: basis

  steps:
    - action: debug:log
